# Points the app at a locally-run backend (see `./gradlew bootRunLocal` in backend/).
# Expo loads this file with higher priority than .env, so this line overrides
# API_BASE_URL while everything else (Google client IDs, etc.) still comes
# from the tracked .env. Delete or comment out to fall back to the deployed API.
API_BASE_URL=http://localhost:8080
